[project]
name = "django-routing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.1.1",
    "easy-password-generator>=0.0.7",
    "gunicorn>=26.2.0",
    "uvicorn>=0.52.4",
]
